export type TAddContactModalTmpProps = {
  cancelButton: string;
  addButton: string;
  loginInput: string;
};

export type TAddContactModalComponentState = {
  //
};

export type TAddContactModalCallbacks = {
  onApply: () => void;
};
